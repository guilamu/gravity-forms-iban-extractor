# gravity-forms-iban-extractor
A WordPress plugin that adds an IBAN (International Bank Account Number) extractor field type to Gravity Forms. Validates IBANs locally and extracts detailed bank information in real-time.
